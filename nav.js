document.getElementById("select-juego").addEventListener("change", function() {
  const target = this.value;
  if (target.startsWith("#")) {
    document.querySelector(target).scrollIntoView({ behavior: "smooth" });
  }
});
document.getElementById("select-carta").addEventListener("change", function() {
  const target = this.value;
  if (target.startsWith("#")) {
    document.querySelector(target).scrollIntoView({ behavior: "smooth" });
  }
});
document.getElementById("select-gamer").addEventListener("change", function() {
  const target = this.value;
  if (target.startsWith("#")) {
    document.querySelector(target).scrollIntoView({ behavior: "smooth" });
  }
});
document.getElementById("select-ropa").addEventListener("change", function() {
  const target = this.value;
  if (target.startsWith("#")) {
    document.querySelector(target).scrollIntoView({ behavior: "smooth" });
  }
});
document.getElementById("select-perifericos").addEventListener("change", function() {
  const target = this.value;
  if (target.startsWith("#")) {
    document.querySelector(target).scrollIntoView({ behavior: "smooth" });
  }
});
document.getElementById("select-teclados").addEventListener("change", function() {
  const target = this.value;
  if (target.startsWith("#")) {
    document.querySelector(target).scrollIntoView({ behavior: "smooth" });
  }
});
document.getElementById("select-mouses").addEventListener("change", function() {
  const target = this.value;
  if (target.startsWith("#")) {
    document.querySelector(target).scrollIntoView({ behavior: "smooth" });
  }
});
document.getElementById("select-audifonos").addEventListener("change", function() {
  const target = this.value;
  if (target.startsWith("#")) {
    document.querySelector(target).scrollIntoView({ behavior: "smooth" });
  }
});
document.getElementById("select-monitores").addEventListener("change", function() {
  const target = this.value;
  if (target.startsWith("#")) {
    document.querySelector(target).scrollIntoView({ behavior: "smooth" });
  }
});
document.getElementById("select-mousepads").addEventListener("change", function() {
  const target = this.value;
  if (target.startsWith("#")) {
    document.querySelector(target).scrollIntoView({ behavior: "smooth" });
  }
});